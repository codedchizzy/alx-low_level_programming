/**
  *_abs - computes the absolute value of an integer
  *@e: the interer to compute absoutw value
  *Return: the absolue value of n
  */
int _abs(int e)
{
	if (e < 0)
	{
		return (-e);
	}
	else
	{
		return (e);
	}
}
