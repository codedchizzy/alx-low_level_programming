#include <stdio.h>
#include <stdlib.h>
/**
  *main - entry point
  *Return: Ends program with 0
  */
int main(void)
{
	int num = 0;

	while (num <= 9)
	{
		printf("%d\n",num);
		num++;
	}
	return (0);
}
