#include <stdio.h>
/**
  *main - Is the the entry point
  *printf - prints the string
  *Return: ends the program with a value of 0
  */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
