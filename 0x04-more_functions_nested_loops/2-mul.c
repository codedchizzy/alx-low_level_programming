#include "main.c"

/**
 * mul - multiplies 2 integers
 * @a: a character
 * @b: character
 * Return: gets the vakue and do the operation
 */

int mul(int a, int b)
{
	int c;

	c = a * b;
	return (c);
}
